[build]
  command = "pnpm build:static"
  publish = "dist/public"

[build.environment]
  VITE_BASE_PATH = "/"
  VITE_STATIC_ONLY = "true"
  VITE_API_BASE_URL = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
